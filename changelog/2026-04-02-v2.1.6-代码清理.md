# v2.1.6 - 代码清理

**日期**：2026-04-02

## 变更内容

### 移除调试代码
- 移除 vConsole 调试工具
- 移除所有 `console.log` 调试日志
- 移除 `fetchFundBasicInfo`、`fetchSingleBatch`、`fetchFundsLive`、`loadFunds`、`loadData` 等函数中的调试代码

### 移除 Mock 数据
- 删除 `src/api/mockData.js` 文件
- 移除 `fetchRealtimeAuto` 中的 Mock 数据降级逻辑

### 移除诊断面板
- 移除 `App.vue` 中的诊断面板模板和样式
- 移除 `useConfig.js` 中的 `configDiag` 相关代码

### 性能优化
- fundgz 请求间隔从 300ms 优化为 100ms
- 频率限制暂停从 2 秒优化为 0.5 秒

### 其他
- `.gitignore` 允许提交 `dist/` 目录
- 卸载 vconsole 依赖

## 文件变更

| 文件 | 变更 |
|------|------|
| `src/main.js` | 移除 vConsole |
| `src/App.vue` | 移除诊断面板、调试日志 |
| `src/api/funds.js` | 移除调试日志、Mock 数据 |
| `src/composables/useFunds.js` | 移除调试日志 |
| `src/composables/useConfig.js` | 移除 configDiag |
| `src/api/mockData.js` | 删除 |
| `.gitignore` | 允许 dist/ |
| `package.json` | 移除 vconsole 依赖 |